import turtle
t=turtle.Turtle()
t.speed(6)

def draw_shape(size,turns):
    for i in range(turns):
        t.forward(size+i*2)
        t.left(144)

def main():
    s=int(input("How many spirals: "))
    for i in range(s):
        draw_shape(50+i*5,5)
        t.left(20)

main()
turtle.done()
